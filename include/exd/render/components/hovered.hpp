#pragma once

namespace exd::render {

/// Marker component — entity is currently under the mouse cursor.
/// Placed/removed by PickerSystem each frame.
struct Hovered {};

} // namespace exd::render
